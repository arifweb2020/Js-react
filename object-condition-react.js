{
              Object.keys(addQueryData).length == 0 ?
                <AddQuery />
                :
                <button className="disableBtn" disabled>{approvalLabel.fieldsLabel.addQuery}</button>
            }
